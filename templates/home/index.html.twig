{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}

    {{ encore_entry_link_tags('wallet') }}
{% endblock %}
{% block javascripts %}
    {{ parent() }}

    {{ encore_entry_script_tags('wallet') }}
{% endblock %}
{% block body %}
    {% block page_contents %}
        <a href="{{ path('app_login') }}">Login</a>
        <br>
        <br>
        <a href="{{ path('app_register') }}">Register</a>
        <br>
        <br>
        <a href="{{ path('connect_facebook_start') }}">Login with Facebook</a>
        <br>
        <br>
        <a href="{{ path('connect_google_start') }}">Login with Google</a>
    {% endblock %}

{% endblock %}